---
collections:
  - name: community.general
    version: ">=10.0.0"

  - name: ansible.posix
    version: ">=1.6.0"

  - name: community.proxmox
    version: ">=1.6.0,<2.0.0"

  # Used by deploy_compose_stack and update_docker_stacks.
  - name: community.docker
    version: ">=4.0.0"
