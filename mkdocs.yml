site_name: NanoNode Homelab

theme:
  name: material

nav:
  - Home: index.md
  - Hardware:
      - Overview: hardware/overview.md
      - Power Delivery: hardware/power-delivery-module.md

markdown_extensions:
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
